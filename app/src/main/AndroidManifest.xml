<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/study_space"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/study_space_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudySpace">

        <activity
            android:name=".auth.GreetingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".auth.GreetRegisteredUser"
            android:exported="false" />

        <activity
            android:name=".main.TaskWindowActivity"
            android:exported="false" />

        <activity
            android:name=".main.AnalyticWindowActivity"
            android:exported="false" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>