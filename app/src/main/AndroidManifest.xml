<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />


    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/study_space"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/study_space_foreground"
        android:supportsRtl="true"
        android:theme="@style/Theme.StudySpace">

        <activity
            android:name=".auth.GreetingActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.SignUpActivity"
            android:exported="false" />

        <activity
            android:name=".main.MainActivity"
            android:exported="false" />

        <activity
            android:name=".auth.GreetRegisteredUser"
            android:exported="false" />

        <activity
            android:name=".task.TaskWindowActivity"
            android:exported="false" />

        <activity
            android:name=".analytic.AnalyticWindowActivity"
            android:exported="false" />

        <activity
            android:name=".focus.FocusWindowActivity"
            android:exported="false"
            android:theme="@style/Theme.StudySpace.FullScreen" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>